steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --build-arg=API_URL=${_API_URL}
      - --build-arg=VACATION_API_URL=${_VACATION_API_URL}
      - --build-arg=NEXT_PUBLIC_GOOGLE_CLIENT_ID=${_GOOGLE_CLIENT_ID}
      - -t=${_IMAGE}
      - .

images:
  - ${_IMAGE}
